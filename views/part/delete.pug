extends ../layout 

block content 
  p Are you sure you want to delete the part 
    b= part.name 
    | ?
  form(method='POST' action='')
    input#id(
      type='hidden'
      name='id'
      required='true'
      value=part._id 
    )
    button(
      type='submit'
    ) Delete